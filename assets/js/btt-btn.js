// -------------------------------------------------------------------- Go To Top Button

window.addEventListener('scroll', function () {
    scroll.classList.toggle('active', window.scrollY > 500);
});
